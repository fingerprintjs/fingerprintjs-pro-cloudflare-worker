{
  "name": "e2e",
  "private": true,
  "description": "E2E tests for the Cloudflare Worker proxy integration",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0"
  }
}
